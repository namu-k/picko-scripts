# Newsletter Pack 프롬프트

다음 콘텐츠를 newsletter 채널용으로 변환해주세요.

**원본**:
- 제목: {{ title }}
- 요약: {{ summary }}

{% set w_out = weekly_outcome | default('') %}
{% set w_cta = weekly_cta | default('') %}
{% if w_out or w_cta %}
**주간 컨텍스트(있을 때만 자연스럽게 반영)**:
- 이번 주 고객 성과: {{ w_out }}
- 이번 주 CTA: {{ w_cta }}

{% endif %}
**하드 제약(반드시 준수)**:
- **전체 본문은 정확히 {{ max_length }}자 이하**여야 합니다. 1자라도 초과하면 실패입니다.
- 톤: {{ tone }}
- 해시태그 사용 설정값: {{ "ON" if use_hashtags else "OFF" }}

**출력 계약(Output Contract)**:
1. 본문만 출력하세요 (제목 라벨, 메타 설명, 따옴표 금지).
2. 마크다운 코드펜스(\`\`\`)를 절대 출력하지 마세요.
3. **해시태그는 절대 출력하지 말 것**. 해시태그는 시스템이 후처리 단계에서 자동으로 붙입니다.
4. 뉴스레터 톤이라도 불필요한 서두를 줄이고 핵심 전달에 집중하세요.

**권장 훅 공식(2~3개 중 하나 선택해서 시작)**:
1. 에디토리얼 오프닝 1-2문장 -> 핵심 불릿 3개 -> 마무리 CTA
2. 이번 주 관찰 한 줄 -> 왜 중요한지 1단락 -> 핵심 불릿 3개 -> 마무리 CTA
3. 독자 문제 공감 한 줄 -> 해결 관점 1단락 -> 핵심 불릿 3개 -> 다음 액션 CTA

newsletter 포스트 본문:
