# Twitter Pack 프롬프트

다음 콘텐츠를 twitter 채널용으로 변환해주세요.

**원본**:
- 제목: {{ title }}
- 요약: {{ summary }}

{% set w_out = weekly_outcome | default('') %}
{% set w_cta = weekly_cta | default('') %}
{% if w_out or w_cta %}
**주간 컨텍스트(있을 때만 자연스럽게 반영)**:
- 이번 주 고객 성과: {{ w_out }}
- 이번 주 CTA: {{ w_cta }}

{% endif %}
**하드 제약(반드시 준수)**:
- **전체 본문은 정확히 {{ max_length }}자 이하**여야 합니다. 1자라도 초과하면 실패입니다.
- 톤: {{ tone }}
- 해시태그 사용 설정값: {{ "ON" if use_hashtags else "OFF" }}

**출력 계약(Output Contract)**:
1. 포스트 본문만 출력하세요.
2. 레이블, 설명문, 따옴표, 마크다운 코드펜스(\`\`\`)를 절대 출력하지 마세요.
3. **해시태그는 절대 출력하지 말 것**. 해시태그는 시스템이 후처리 단계에서 자동으로 붙입니다.
4. 줄바꿈은 가독성 범위에서만 사용하고, 불필요한 공백으로 글자 수를 낭비하지 마세요.

**권장 훅 공식(2~3개 중 하나 선택해서 시작)**:
1. 반대 관점 오프너 -> 핵심 인사이트 1개 -> 바로 실행할 행동 1개 -> 질문/CTA
2. 호기심 유발 한 줄 -> 왜 중요한지 한 줄 -> 실행 포인트 1개 -> 질문/CTA
3. 오해 깨기 한 줄 -> 데이터/사실 1개 -> 내일부터 할 액션 1개 -> 질문/CTA

twitter 포스트 본문:
